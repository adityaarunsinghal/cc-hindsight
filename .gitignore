node_modules
dist
coverage
*.tgz
pack.json
.DS_Store

# Local-only working notes and session memory (never publish)
log.md
.remember/

# Demo GIF workshop (local-only; the shipped GIF lives in .github/assets/)
demo/

# Launch copy drafts (local-only working dir; nothing here ships)
launch/

# Local verification scratch (golden-reference exports, install logs). Never
# upstream: these hold real transcript content and would drown `biome check .`
# (useIgnoreFile: true means biome honors this file).
.golden-baseline/
.golden-*/
npm-install.log
